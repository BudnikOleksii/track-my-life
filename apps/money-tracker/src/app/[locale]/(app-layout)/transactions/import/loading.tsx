import { PageSkeleton } from '../../components/page-skeleton/PageSkeleton';

const ImportTransactionsLoading = () => <PageSkeleton count={4} height={48} />;

export default ImportTransactionsLoading;
